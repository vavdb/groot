namespace Groot.UI.Theme;

/// <summary>The four phone-nav destinations. Order here is left-to-right in the bottom nav.</summary>
public enum GrootDestination
{
    Home,
    Lift,
    Run,
    Progress,
}
