@page "/run"
@using Groot.Core.Programs
@using Groot.UI.Components
@using Groot.UI.Audio
@using Groot.Web.Audio
@inject ICuePlayer Cues

<PageTitle>Run · Groot</PageTitle>

<GrootRunScene Programs="ProgramCatalog.Embedded.IntervalPrograms"
               CuePlayer="Cues"
               ShowProgram="true"
               ShowAudioControls="true"
               AudioChanged="OnAudioChanged" />

@code {
    /// <summary>The browser wants a full BCP 47 tag; the programs speak two-letter codes.</summary>
    private void OnAudioChanged(GrootAudioState state)
    {
        if (Cues is WebCuePlayer web) web.Language = state.Language == "nl" ? "nl-NL" : "en-GB";
    }
}
