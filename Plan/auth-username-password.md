---
status: "backlog"
tags: [Auth, Backend]
docs: research.md
hook: MVP auth — username(+password) only, zero PII; depends on backend-path-spike
order: 36
---
# Auth: username/password, zero PII

MVP auth is username(+password) only, no PII collected. Later: Google
sign-in (Android) / Apple ID (iOS). Depends on the backend choice
(`backend-path-spike`) — Supabase solves this mostly for free; a
self-hosted API means building it by hand.

* Links: `research.md` §5, §5.4
